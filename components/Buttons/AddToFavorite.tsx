import { Button } from "@mui/material";

const AddToFavorite = () => {
  return (
    <Button size="large" variant="outlined">
      Add to favorite
    </Button>
  );
};

export default AddToFavorite;
